{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/frankxai/human-mind-intelligence-system/schemas/emotion.schema.json",
  "title": "Emotion Observation",
  "description": "An observation of emotion: affective states involving physiological arousal, cognitive appraisal, and behavioral tendencies. Models what is observed and keeps interpretation strictly separate. Non-clinical: never a diagnosis.",
  "type": "object",
  "required": [
    "construct",
    "observation",
    "confidence"
  ],
  "additionalProperties": false,
  "properties": {
    "schemaVersion": {
      "type": "string",
      "const": "0.1.0",
      "description": "Version of the human-mind schema family this document conforms to."
    },
    "construct": {
      "type": "string",
      "const": "emotion",
      "description": "The canonical module this observation is about. Canon: mind-intelligence-systems/models/human-mind/emotion.md"
    },
    "observation": {
      "type": "string",
      "minLength": 1,
      "description": "What was actually observed, in plain language. Description only \u2014 no inference, no label. Example: 'Voice rose and pace quickened when the deadline was mentioned.'"
    },
    "indicators": {
      "type": "array",
      "description": "Discrete, observable markers drawn from the emotion construct's key processes. Each is a thing you can point at, not a conclusion.",
      "items": {
        "type": "string",
        "enum": [
          "appraisal",
          "expression",
          "regulation",
          "emotional-contagion",
          "mood",
          "affect",
          "high-arousal",
          "low-arousal",
          "positive-valence",
          "negative-valence",
          "approach-tendency",
          "avoidance-tendency"
        ]
      },
      "uniqueItems": true,
      "default": []
    },
    "context": {
      "type": "object",
      "description": "Situational frame for the observation. Helps later interpretation without contaminating the observation itself.",
      "additionalProperties": false,
      "properties": {
        "setting": {
          "type": "string",
          "description": "Where/when this was observed."
        },
        "task": {
          "type": "string",
          "description": "What the person was doing."
        },
        "duration": {
          "type": "string",
          "description": "Observation window, e.g. ISO 8601 duration 'PT10M'."
        }
      }
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Confidence in the OBSERVATION being accurate (not in any interpretation). 0 = unsure it happened, 1 = directly and reliably observed."
    },
    "interpretation": {
      "type": "object",
      "description": "OPTIONAL and strictly separate from observation. A labelled hypothesis about what the observation might mean. Never a diagnosis. Always carries its own evidence and confidence.",
      "additionalProperties": false,
      "required": [
        "hypothesis",
        "confidence"
      ],
      "properties": {
        "hypothesis": {
          "type": "string",
          "description": "A tentative, falsifiable reading. Example: 'Arousal may be rising in response to a perceived time threat.'"
        },
        "lens": {
          "type": "string",
          "description": "Which framework informs the reading, if any.",
          "enum": [
            "basic-emotions",
            "dimensional-valence-arousal",
            "appraisal-theory",
            "constructed-emotion",
            "none"
          ]
        },
        "evidence": {
          "type": "array",
          "description": "Pointers back to the observation(s) and any external support for the hypothesis.",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "alternatives": {
          "type": "array",
          "description": "Other readings that fit the same observation. Keeps the interpretation honest.",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "confidence": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Confidence in the INTERPRETATION. Should typically be lower than observation confidence."
        }
      }
    },
    "provenance": {
      "type": "object",
      "description": "Where this observation came from and who recorded it.",
      "additionalProperties": false,
      "properties": {
        "source": {
          "type": "string",
          "enum": [
            "self-report",
            "third-party-report",
            "agent-inference",
            "instrument",
            "behavioral-log",
            "unknown"
          ],
          "default": "unknown"
        },
        "observer": {
          "type": "string",
          "description": "Identifier for who/what made the observation."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "When the observation was recorded (ISO 8601)."
        },
        "reliability": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Trust in the source itself, independent of this single observation."
        }
      }
    }
  }
}
